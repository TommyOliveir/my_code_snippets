class Animal {
  constructor(data) {
    (this.name = data.name), (this.type = data.type);
  }
  greet() {
    return `the animal ${this.name} is a ${this.type}`;
  }
}

const animal1 = new Animal({ name: "eagle", type: "bird" });
console.log(animal1.greet());
